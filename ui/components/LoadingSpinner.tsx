import React from "react";
import {
  LoadingContainer,
  LoadingSpinner,
} from "ui/styles/loadingSpinner.styles";

const LoadingSpinnerComponent = () => {
  return (
    <LoadingContainer>
      <LoadingSpinner />
    </LoadingContainer>
  );
};

export default LoadingSpinnerComponent;
